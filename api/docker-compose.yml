services:
  currency-converter:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
      - GOOGLE_APPLICATION_CREDENTIALS=/tmp/keys/adc.json
      - PROJECT_ID=joy-currency-conversion
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=app
      - DB_PASSWORD=app_password
      - DB_NAME=currency_conversion
    volumes:
      # Montar las credenciales de tu máquina al contenedor
      - $HOME/.config/gcloud/application_default_credentials.json:/tmp/keys/adc.json:ro
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=root_password
      - MYSQL_DATABASE=currency_conversion
      - MYSQL_USER=app
      - MYSQL_PASSWORD=app_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:

